---
phase: 01-provider-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/services/GeminiProvider.ts
  - src/main/services/PromptOptimizer.ts
  - src/renderer/src/store/useExamConfigStore.ts
  - src/renderer/src/pages/ReviewConfirmationPage.tsx
autonomous: true
requirements: [PM-04, PM-05]

must_haves:
  truths:
    - "GeminiProvider.ts and PromptOptimizer.ts no longer exist in the codebase"
    - "useExamConfigStore only exposes 4 question types (MCQ, T/F, Fill-in-blanks, Short Answer)"
    - "MAX_TOTAL_ITEMS is 50, not 100"
    - "QUESTION_TYPES constant has exactly 4 entries"
    - "ReviewConfirmationPage does not reference essay, matching, or identification"
  artifacts:
    - path: "src/renderer/src/store/useExamConfigStore.ts"
      provides: "Simplified exam config with 4 types and 50-question cap"
      contains: "MAX_TOTAL_ITEMS: 50"
    - path: "src/renderer/src/pages/ReviewConfirmationPage.tsx"
      provides: "Review page with no references to removed question types"
  key_links:
    - from: "src/renderer/src/store/useExamConfigStore.ts"
      to: "src/shared/types/exam.ts"
      via: "QuestionType type import alignment"
      pattern: "multipleChoice.*trueFalse.*fillInTheBlanks.*shortAnswer"
---

<objective>
Remove dead code (GeminiProvider, PromptOptimizer) and simplify the frontend to support only 4 question types with a 50-question maximum.

Purpose: Reduce codebase complexity by deleting unused providers and question types that were never effective. Align the UI with the research finding that 50 questions covering 50 unique concepts is optimal.

Output: Deleted files (GeminiProvider.ts, PromptOptimizer.ts), simplified useExamConfigStore.ts and ReviewConfirmationPage.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@RESEARCH.md (section 3: Question Type Scope, section 4: Optimal Question Limits)
@src/main/services/GeminiProvider.ts
@src/main/services/PromptOptimizer.ts
@src/renderer/src/store/useExamConfigStore.ts
@src/renderer/src/pages/ReviewConfirmationPage.tsx

<interfaces>
<!-- The executor needs to know what imports to check for when removing dead code -->

Files that import from GeminiProvider.ts (must be updated/cleaned):
- src/main/services/ProviderFactory.ts: `import { GeminiProvider } from './GeminiProvider'`

Files that import from PromptOptimizer.ts:
- None found in IPC handlers (it is standalone, not wired into production flow)

Files that reference essay/matching/identification question types:
- src/renderer/src/store/useExamConfigStore.ts: QuestionType union, QUESTION_TYPES constant, initialQuestionTypes
- src/shared/types/exam.ts: GeneratedQuestion.type union (handled by Plan 01)
- src/main/services/GroqProvider.ts: ExamGenerationConfig (will be replaced by Plan 01's TogetherProvider)
- src/main/services/GeminiProvider.ts: formatQuestionTypes method (being deleted)

Current useExamConfigStore constants to modify:
```typescript
export const EXAM_CONFIG_RULES = {
  MIN_TOTAL_ITEMS: 10,
  MAX_TOTAL_ITEMS: 100,  // -> change to 50
  MIN_ITEMS_PER_TYPE: 0,
  MAX_ITEMS_PER_TYPE: 100,  // -> change to 50
}
```

Current QUESTION_TYPES has 7 entries (essay, matching, identification must be removed).
Current initialQuestionTypes has 7 keys (essay, matching, identification must be removed).
Current QuestionType has 7 values (essay, matching, identification must be removed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete GeminiProvider.ts and PromptOptimizer.ts</name>
  <files>src/main/services/GeminiProvider.ts, src/main/services/PromptOptimizer.ts</files>
  <read_first>
    - src/main/services/GeminiProvider.ts (confirm it exists and is the file to delete)
    - src/main/services/PromptOptimizer.ts (confirm it exists and is the file to delete)
    - src/main/services/ProviderFactory.ts (check imports from GeminiProvider)
    - src/main/index.ts (check imports from PromptOptimizer or GeminiProvider)
  </read_first>
  <action>
1. Delete `src/main/services/GeminiProvider.ts` entirely using `rm`.

2. Delete `src/main/services/PromptOptimizer.ts` entirely using `rm`.

3. After deletion, search the entire `src/` directory for any remaining imports of these files:
   - `grep -r "GeminiProvider" src/`
   - `grep -r "PromptOptimizer" src/`

   If any imports are found in files OTHER than ProviderFactory.ts (which will be rewritten in Plan 03), comment them out or remove the import line. ProviderFactory.ts will be fully rewritten in Plan 03, so leave it as-is for now -- it will have type errors temporarily.

4. Also search for and note any references to `@google/generative-ai` in import statements (this dependency will be removed from package.json in Plan 03).
  </action>
  <verify>
    <automated>cd "C:/Users/NITRO V15/Documents/Development-New/Projects/Active/qreate" && test ! -f src/main/services/GeminiProvider.ts && test ! -f src/main/services/PromptOptimizer.ts && echo "DELETED OK" || echo "FILES STILL EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - src/main/services/GeminiProvider.ts does not exist on disk
    - src/main/services/PromptOptimizer.ts does not exist on disk
    - No file in src/ (other than ProviderFactory.ts) contains `import.*GeminiProvider`
    - No file in src/ contains `import.*PromptOptimizer`
  </acceptance_criteria>
  <done>GeminiProvider.ts and PromptOptimizer.ts are deleted, no dangling imports remain outside ProviderFactory.ts</done>
</task>

<task type="auto">
  <name>Task 2: Simplify useExamConfigStore to 4 types and 50-question cap</name>
  <files>src/renderer/src/store/useExamConfigStore.ts</files>
  <read_first>
    - src/renderer/src/store/useExamConfigStore.ts
    - src/shared/types/exam.ts (to confirm the 4-type QuestionType from Plan 01)
  </read_first>
  <action>
Modify `src/renderer/src/store/useExamConfigStore.ts`:

1. Change the `QuestionType` type to only 4 values:
```typescript
export type QuestionType =
  | 'multipleChoice'
  | 'trueFalse'
  | 'fillInTheBlanks'
  | 'shortAnswer'
```

2. Remove entries for `essay`, `matching`, and `identification` from the `QUESTION_TYPES` constant. The constant should have exactly 4 entries:
```typescript
export const QUESTION_TYPES: Record<QuestionType, { label: string; description: string; icon: string }> = {
  multipleChoice: {
    label: 'Multiple Choice',
    description: 'Questions with multiple answer options, one correct answer',
    icon: 'check-circle',
  },
  trueFalse: {
    label: 'True/False',
    description: 'Questions with true or false answers',
    icon: 'scale',
  },
  fillInTheBlanks: {
    label: 'Fill in the Blanks',
    description: 'Questions with missing words to complete',
    icon: 'text-cursor',
  },
  shortAnswer: {
    label: 'Short Answer',
    description: 'Questions requiring brief written responses',
    icon: 'pencil',
  },
}
```

3. Update `EXAM_CONFIG_RULES` to cap at 50:
```typescript
export const EXAM_CONFIG_RULES = {
  MIN_TOTAL_ITEMS: 10,
  MAX_TOTAL_ITEMS: 50,
  MIN_ITEMS_PER_TYPE: 0,
  MAX_ITEMS_PER_TYPE: 50,
}
```

4. Remove `essay`, `matching`, and `identification` from `initialQuestionTypes`:
```typescript
const initialQuestionTypes: Record<QuestionType, number> = {
  multipleChoice: 0,
  trueFalse: 0,
  fillInTheBlanks: 0,
  shortAnswer: 0,
}
```

5. Remove the `essay`, `matching`, and `identification` keys from the `questionTypes` state type in `ExamConfigState` interface. The `questionTypes` field should be `Record<QuestionType, number>` which now only has 4 keys.

6. Keep all other logic (difficulty distribution, validation, auto-distribute) unchanged.
  </action>
  <verify>
    <automated>cd "C:/Users/NITRO V15/Documents/Development-New/Projects/Active/qreate" && grep -c "essay\|matching\|identification" src/renderer/src/store/useExamConfigStore.ts && echo "FOUND REMOVED TYPES" || echo "CLEAN - no removed types"</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/store/useExamConfigStore.ts contains `MAX_TOTAL_ITEMS: 50`
    - src/renderer/src/store/useExamConfigStore.ts contains `MAX_ITEMS_PER_TYPE: 50`
    - src/renderer/src/store/useExamConfigStore.ts does NOT contain the string `essay`
    - src/renderer/src/store/useExamConfigStore.ts does NOT contain the string `matching`
    - src/renderer/src/store/useExamConfigStore.ts does NOT contain the string `identification`
    - QUESTION_TYPES constant has exactly 4 entries (multipleChoice, trueFalse, fillInTheBlanks, shortAnswer)
    - initialQuestionTypes has exactly 4 keys
  </acceptance_criteria>
  <done>useExamConfigStore has only 4 question types and 50-question cap</done>
</task>

<task type="auto">
  <name>Task 3: Clean ReviewConfirmationPage of removed question type references</name>
  <files>src/renderer/src/pages/ReviewConfirmationPage.tsx</files>
  <read_first>
    - src/renderer/src/pages/ReviewConfirmationPage.tsx
    - src/renderer/src/store/useExamConfigStore.ts (after Task 2 modifications)
  </read_first>
  <action>
Read `src/renderer/src/pages/ReviewConfirmationPage.tsx` fully and search for any references to:
- `essay` (as a question type string or variable)
- `matching` (as a question type string or variable)
- `identification` (as a question type string or variable)
- `Essay` (capitalized in labels/display)
- `Matching` (capitalized in labels/display)
- `Identification` (capitalized in labels/display)

For each found reference:
- If it is in a type annotation, update to use the simplified QuestionType
- If it is in a conditional rendering block (like an if/switch for question types), remove the case
- If it is in a list/array of question types, remove the entry

Also check for any hardcoded maximum like `100` that should now be `50` in display text or validation logic.

If the file has no references to removed types (they may all come from the store dynamically), verify this is the case and note it. The file may already be clean if it reads question types dynamically from QUESTION_TYPES constant.

Additionally, search these other renderer files for removed question type references and clean them:
- `grep -r "essay\|matching\|identification" src/renderer/src/` (excluding node_modules)

If any other page or component files reference the removed types, clean those too.
  </action>
  <verify>
    <automated>cd "C:/Users/NITRO V15/Documents/Development-New/Projects/Active/qreate" && grep -r "essay\|matching\|identification" src/renderer/src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | head -20; echo "---"; grep -r "MAX_TOTAL_ITEMS.*100\|max.*100.*question" src/renderer/src/ --include="*.ts" --include="*.tsx" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - No .ts or .tsx file in src/renderer/src/ contains the strings `essay`, `matching`, or `identification` as question type references
    - No file in src/renderer/src/ contains `MAX_TOTAL_ITEMS: 100` or displays "100" as a question limit
    - ReviewConfirmationPage.tsx compiles without type errors
  </acceptance_criteria>
  <done>All renderer files are free of removed question type references and updated to 50-question cap</done>
</task>

</tasks>

<verification>
- GeminiProvider.ts and PromptOptimizer.ts do not exist
- `grep -r "essay\|matching\|identification" src/renderer/src/` returns no results
- `grep "MAX_TOTAL_ITEMS" src/renderer/src/store/useExamConfigStore.ts` shows 50
- QUESTION_TYPES in useExamConfigStore has exactly 4 entries
</verification>

<success_criteria>
- Dead code deleted (GeminiProvider.ts, PromptOptimizer.ts)
- Frontend supports exactly 4 question types
- Question limit capped at 50
- No dangling references to removed types in any renderer file
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-migration-to-together-ai-qwen3-235b-with-two-pass-generation-dead-code-removal-and-50-question-simplification/01-02-SUMMARY.md`
</output>
