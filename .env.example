# Qreate Environment Configuration Example
# Copy this file to .env.local and fill in your actual values

# =================================================================
# GROQ API CONFIGURATION (Required)
# =================================================================
# Get your free API key from: https://console.groq.com/keys
# Create account with email - no credit card required
# Free tier: 14,400 requests/day (supports ~1,440 users/day)
GROQ_API_KEY=gsk_your_api_key_here

# =================================================================
# USER USAGE LIMITS (Backend Configuration)
# =================================================================
# Daily exam generation limit per user (default: 10)
EXAMS_PER_DAY=10

# Monthly exam generation limit per user (default: 100)
EXAMS_PER_MONTH=100

# Minimum questions per exam (default: 10)
MIN_QUESTIONS_PER_EXAM=10

# Maximum questions per exam (default: 100)
MAX_QUESTIONS_PER_EXAM=100

# =================================================================
# GROQ RATE LIMITING (Global Configuration)
# =================================================================
# Groq API allows 30 req/min, we use 25 for safety buffer
GROQ_REQUESTS_PER_MINUTE=25

# Groq API allows 14,400 req/day, we use 14,000 for safety buffer
GROQ_REQUESTS_PER_DAY=14000

# =================================================================
# DEVELOPMENT SETTINGS
# =================================================================
# Enable detailed logging (true/false)
DEBUG_MODE=true

# Database file location (optional, defaults to app data folder)
# DB_PATH=./qreate-dev.db